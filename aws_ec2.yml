plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1
filters:
  tag:Role:
    - Active-Sonarqube
    - Passive-SonarQube
  instance-state-name: running
keyed_groups:
  - key: tags.Role
    prefix: sonar
